const array = ["b", "b", "c", "a", "d", "a"];
const ob = array.findIndex((item) => item === "ff");
console.log(ob);

//It returns the first occured value index which satisfies the provided testing function else retuns -1.
