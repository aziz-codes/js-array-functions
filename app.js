const array = ["b", "b", "c", "a", "d", "a"];
const ob = array.find((item) => item === "a");
console.log(ob);

//It returns the first occured value which satisfies the provided testing function else retuns undefined.
